kind = "web"
previewPath = "/"
title = "OSINT Terminal"
version = "1.0.0"
id = "artifacts/osint-terminal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23749

[services.development]
run = "pnpm --filter @workspace/osint-terminal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/osint-terminal", "run", "build" ]
publicDir = "artifacts/osint-terminal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23749"
BASE_PATH = "/"
